import React from 'react'

const Card = (weather: number) => {
    
    //import icons 
        //use weather to filter icon
        //use weather to show rain, snow effect using framer motion

  return (
    <div>Card</div>
  )
}

export default Card